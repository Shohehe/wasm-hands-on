apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: containers
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: axum-crm-order-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8002
        env:
        - name: DATABASE_URL
          value: "postgres://crm:crm@postgres.db.svc.cluster.local:5432/crm_containers?sslmode=disable"
        - name: CUSTOMER_SERVICE_URL
          value: "http://customer-service.containers.svc.cluster.local"
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8002
          initialDelaySeconds: 0
          periodSeconds: 1
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: containers
spec:
  selector:
    app: order-service
  ports:
  - port: 80
    targetPort: 8002
